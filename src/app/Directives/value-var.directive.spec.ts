import { ValueVarDirective } from './value-var.directive';

describe('ValueVarDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueVarDirective();
    expect(directive).toBeTruthy();
  });
});
